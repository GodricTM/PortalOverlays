# Diagnostics

Use this folder for raw device captures from Portal hardware, including UI hierarchy dumps, ad hoc
screenshots, and similar one-off inspection files.

Keep these out of the repo root so release assets and source files stay easy to scan.
