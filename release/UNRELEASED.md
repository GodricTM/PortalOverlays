# Portal Overlays — unreleased

**Not published yet.** This file tracks what is built on `main` but not tagged as a GitHub release.
When you ship, copy the relevant sections into `release/RELEASE_NOTES.md`, bump `versionName` /
`versionCode` in `app/build.gradle.kts`, update `version.json`, and tag.

_Nothing pending — v1.8 shipped 2026-07-01. Add new highlights here as they land on `main`._
